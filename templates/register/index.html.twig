{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
<div class="container mt-3">
<h1>Inscription</h1>
    {% if notification %}
        <div class="">{{ notification }}</div>
    {% endif %} 
    {{ form_start(form) }}
    {{ form_end(form) }}
</div>

{% endblock %}
